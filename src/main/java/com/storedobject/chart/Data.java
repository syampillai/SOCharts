package com.storedobject.chart;

public class Data extends AbstractData<Number> {

    /**
     * Constructor.
     *
     * @param data Initial data to add
     */
    public Data(Number... data) {
        super(Number.class, data);
    }
}
